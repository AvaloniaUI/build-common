<Project>
  <PropertyGroup>
    <IsPackable>true</IsPackable>
    <Authors>AvaloniaUI OÜ</Authors>
    <Copyright>Copyright 2019-$([System.DateTime]::Now.ToString(`yyyy`)) &#169; AvaloniaUI OÜ</Copyright>

    <PackageProjectUrl>https://avaloniaui.net</PackageProjectUrl>
    <PackageIcon>icon.png</PackageIcon>

    <_FullReadmePath>$(MSBuildProjectDirectory)/README.md</_FullReadmePath>
    <PackageReadmeFile Condition="Exists('$(_FullReadmePath)')">README.md</PackageReadmeFile>

    <ContinuousIntegrationBuild Condition="'$(GITHUB_ACTIONS)' == 'true'">true</ContinuousIntegrationBuild>
    <Deterministic>true</Deterministic>
    <DebugType Condition="$(ContinuousIntegrationBuild) == 'true'">embedded</DebugType>
  </PropertyGroup>

  <ItemGroup Label="PackageIcon">
    <None Include="$(MSBuildThisFileDirectory)/../branding/icon.png" Pack="true" Visible="false" PackagePath=""/>
    <None Condition="Exists('$(_FullReadmePath)')" Include="$(_FullReadmePath)" Pack="true" PackagePath="\"/>
  </ItemGroup>
</Project>
